
## **🔍 현재 프로젝트 파악 중심 인계 질문리스트**

### **🏗️ 시스템 아키텍처 파악 (최우선)**

#### **1. 현재 구현된 기능들의 실제 동작 상태**

```
구체적 확인 질문:
❓ "STL 로딩 → 3D 시각화 → 객체 선택까지의 전체 플로우가 정상 동작하나요?"
❓ "웹 뷰어에서 스트림라인 시각화와 파라미터 조작이 실제로 작동하나요?"
❓ "현재 어떤 크기까지의 STL 파일을 처리할 수 있나요?"

실습 확인:
✅ pipenv run python -m editor 실행 테스트
✅ 샘플 STL 파일 로딩 테스트  
✅ 웹 뷰어 실행 및 인터랙션 테스트
```

#### **2. 주요 라이브러리 선택 배경과 활용 방식**

```
질문 예시:
❓ "PyVista를 선택한 이유는? VTK 직접 사용 대비 장점은?"
❓ "GSTools의 가우시안 모델 파라미터들(var=3, len_scale=1.5)은 어떤 기준으로 설정했나요?"
❓ "Trame 웹 프레임워크를 도입한 목적은? 단순 시각화인가요, 아니면 협업 도구인가요?"

코드 분석 관점:
model = gs.Gaussian(dim=3, var=3, len_scale=1.5)  # 이 수치의 의미?
view = vtk.VtkRemoteLocalView(...)  # Local vs Remote 모드 선택 기준?
```

#### **3. 데스크톱 앱과 웹 뷰어 연동 방식**

```
질문 예시:
❓ "editor/와 vtk-viewer/가 분리된 이유는?"
❓ "데스크톱에서 만든 모델을 웹 뷰어로 전달하는 방법이 있나요?"
❓ "향후 하나로 통합할 계획인가요?"

현재 구조 파악:
/editor       - PySide6 데스크톱 앱 (모델링)
/vtk-viewer   - Trame 웹 앱 (시뮬레이션 결과)
→ 데이터 교환 방식은?
```


---

### **⚙️ 기능 요구사항 파악 (최우선)**

#### **5. 사용자 시나리오와 워크플로우**

```
질문 예시:
❓ "전형적인 사용자 워크플로우는? (STL 로딩 → ? → 시뮬레이션 → ?)"
❓ "주요 사용자층은? (연구자, 엔지니어, 학생?)"
❓ "한 번의 작업 세션에서 처리하는 모델의 규모는?"

사용 시나리오 예시:
1. 건물 STL 모델 로딩
2. 바람 조건 설정  
3. 시뮬레이션 실행
4. 결과 시각화 및 분석
5. 리포트 생성/내보내기
→ 이 플로우가 맞나요?
```


### **📊 데이터 구조 파악 (최우선)**

#### **8. 프로젝트 파일 저장 구조**

```
질문 예시:
❓ "프로젝트를 저장할 때 포함되어야 할 정보는?"
   - 3D 모델 데이터
   - 시뮬레이션 파라미터  
   - 카메라 위치/뷰포트
   - 재료 속성
   - 경계 조건
❓ "JSON, XML, 바이너리 중 어떤 포맷을 선호하나요?"

현재 미구현:
def on_save_project(self) -> None: pass
→ 저장해야 할 데이터 구조는?
```

#### **9. 시뮬레이션 파라미터 스키마**

```
질문 예시:
❓ "입자 속성에는 어떤 파라미터들이 포함되나요?"
❓ "경계 조건(생성판, 소멸판) 설정 방식은?"
❓ "바람 조건은 어떻게 정의하나요?"

UI와 연결:
def on_particle_properties(self) -> None: pass  # 어떤 UI?
def on_particle_source(self) -> None: pass      # 어떤 파라미터?
```

#### **10. 테스트 데이터셋**

```
질문 예시:
❓ "현재 있는 wind.vtk 외에 다른 테스트 데이터가 있나요?"
❓ "검증용 레퍼런스 결과나 벤치마크 데이터가 있나요?"
❓ "다양한 크기/복잡도의 STL 테스트 파일들이 있나요?"

필요한 테스트 케이스:
- 단순 형상 (박스, 구)
- 복잡한 건물 모델
- 대용량 메시
- 에러 케이스 (손상된 파일)
```

---

### **💻 구현 상태 파악 (중간 우선순위)**

#### **11. 현재 동작 기능들의 테스트 방법**

```
질문 예시:
❓ "개발 환경에서 정상 동작을 확인하는 방법은?"
❓ "STL 로딩이 성공했는지 판단하는 기준은?"
❓ "스트림라인 생성 결과가 올바른지 검증하는 방법은?"

실제 테스트 시나리오:
1. pipenv install → pipenv run python -m editor
2. File → STL Import → 샘플 파일 선택
3. 3D 뷰에서 객체 표시 확인
4. 네비게이터에서 객체 선택 → Transform widget 표시 확인
```

#### **12. 알려진 제한사항들**

```
질문 예시:
❓ "현재 크래시를 유발하는 조건들이 있나요?"
❓ "특정 OS나 Python 버전에서 문제가 있나요?"
❓ "메모리 부족이나 성능 저하가 발생하는 시점은?"

코드에서 보이는 잠재적 문제:
mesh: PolyData = cast(pv.PolyData, pv.get_reader(filename).read())
→ 예외 처리 없음, 파일이 깨졌을 때?
```

---

### **🌐 오픈소스 준비사항 (중간 우선순위)**

#### **13. 라이선스 호환성**

```
확인 필요한 라이브러리들:
✅ PySide6 (LGPL v3) - 상업적 사용시 제약
✅ PyVista (MIT) - 자유로운 사용
✅ GSTools (LGPL v3) - 소스 공개 의무
✅ Trame (Apache 2.0) - 자유로운 사용
✅ VTK (BSD) - 자유로운 사용
```


---

## **🎯 우선순위별 정리**

### **즉시 확인 필요 (1차 미팅)**

1. **실제 동작하는 기능 범위**
2. **사용자 워크플로우**
3. **프로젝트 파일 구조**
4. **라이선스 호환성**

### **개발 착수 전 확인 (2차 미팅)**

5. **성능 요구사항**
6. **테스트 방법**
7. **도메인 정확성**
8. **배포 방식**
