# VSMR 변경이력: 1b34bff → bf71755

커밋 2건 (총 10개 파일, +586 / -161)

| 커밋 | 설명 |
|------|------|
| 5a61f14 | QuickRun 검증 플로우 및 UI 개선 |
| bf71755 | precice_mars.nml URL 지원 및 Build 요청 로깅 |

## 1. 입력 파일 검증(Validation) 시스템 신규 추가

- Proto 스키마 변경 (inputd.proto): ValidationError 메시지 타입 제거 → repeated string messages로 단순화
- Stub 반영 (inputd_pb.ts): ValidationError 클래스 제거, errors → messages 필드로 변경
- 서비스 신규 (inputdService.ts): validateInputContent() RPC 클라이언트 구현
- 검증 결과 다이얼로그 신규 (InputValidationResultDialog.tsx): PASS/FAIL 결과를 모노스페이스 로그로 표시

## 2. QuickRun 검증 플로우 대폭 개선 (useCoSimQuickRun.ts)

- 2단계 실행 구조: 검증(validateMutation) → 실행(startMutation) 분리
- 모델별 검증 상태 관리: validationByModel (idle/validating/valid/invalid), validatedInputs 캐싱
- 입력 해시 기반 캐싱: FNV-1a 해시로 입력 파일 변경 감지, 미변경 시 재검증 스킵
- InputValidationFailedError 커스텀 에러 클래스 추가
- 파일 추가/삭제/선택 변경 시 해당 모델의 검증 상태 자동 초기화
- canExecute 플래그 추가 (검증 통과 여부)

## 3. precice_mars.nml URL 지원

- useCoSimQuickRun.ts, useSimulationData.ts: 업로드 시 precice_mars.nml 파일 URL을 별도 추출 → Build 요청 args에 3번째 인자로 전달
- 단일/멀티 모델 모두 [taskMode, inputFileUrl, preciceMarsNmlUrl?] 형태로 통일

## 4. 시뮬레이션 히스토리 로드 로직 이동 (ProjectHomePage.tsx → SimulationPage.tsx)

- ProjectHomePage: plotfl 다운로드/파싱 로직 전부 제거 → 단순히 navigate(historyViewUrl)만 수행
- SimulationPage (AnalysisView): requestedSimulationId query param으로 자동 로드
- 멀티 모델: taskIndex 0~N-1 순회하며 각 모델 결과 로드
- 단일 모델/quickrun: taskIndex 0 fallback
- requestedHistoryLoadRef로 중복 로드 방지

## 5. TaskListPanel 히스토리 캐싱 (TaskListPanel.tsx)

- 5초 TTL 메모리 캐시 + in-flight 중복 요청 병합
- 새로고침 버튼은 force=true로 캐시 무시

## 6. 기타 개선

- 에러 toast 메시지에서 상세 에러 내용 노출 제거 (보안/UX)
- retry: false 추가로 mutation 자동 재시도 방지
- QuickRunDialog.tsx: 검증 실패 시 InputValidationResultDialog 자동 표시
